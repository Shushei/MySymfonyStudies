{# empty Twig template #}
{% extends 'MaciejBundle:Default:Default.html.twig' %}

{% block title %}Form{% endblock %}

    
{% block body %} 

{{ form_start(form) }}
{{ form_widget(form) }}
{% if not changed.clogo is empty %} <img src="{{asset('/uploads/companies/' ~ changed.clogo.filename)}}">
        <a href="{{ path('maciej_companydeleteimage', {'wild' : changed.id})}}">Delete Image </a>{% endif %}
        <input type="submit" value="Edit"
               class="btn btn-default pull-right"/>
{{ form_end(form) }}


{% endblock %}  
{% extends 'MaciejBundle:Switch:Switch.html.twig' %}

{% set active = 'List of Games'%}

{% block title %}List{% endblock %}

    
{% block body %} 
    <table>
        <tr>
            <td>id</td>
            <td>Title</td>
            <td>Company name</td>
            <td> Date </td>
        </tr>
    {% for id in games %}
        <tr>
        <td> {{ id.id}} </td>
        <td> {{id.title}} </td> 
        <td> {{id.company}} </td>
        <td> {{id.releaseDate.format('Y-m-d')}} </td>
        <td>  <a href="{{ path('maciej_list', {'wild' : id.id})}}">Delete</a> </td>
        <td>  <a href="{{ path('maciej_edit', {'wild' : id.id})}}">Edit</a> </td>
        </tr>
    {% endfor %}
 
    </table>
   
   
    
{% endblock %}  {# empty Twig template #}

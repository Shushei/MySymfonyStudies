{# empty Twig template #}

        
  <!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Test Application{% endblock %}</title>
    </head>
    <body>
        <div id="sidebar">
            {% block sidebar %}
                <ul>
                    <li> <a href="{{ path('maciej_first')}}">First</a></li>
                    <li> <a href="{{ path('maciej_second')}}">Second</a></li>
                    <li> <a href="{{ path('maciej_form')}}">Form</a></li>
                     <li> <a href="{{ path('maciej_list')}}">List of Games</a></li>
                </ul>
            {% endblock %}
        </div>

        <div id="content">
            {% block body %}{% endblock %}
        </div>
    </body>
</html>     
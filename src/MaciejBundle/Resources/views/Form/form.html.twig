{% extends 'MaciejBundle:Switch:Switch.html.twig' %}

{% block title %}Form{% endblock %}
{% block sidebar %}
                <ul>
                   <li> <a href="{{ path('maciej_first')}}">First</a></li>
                    <li> <a href="{{ path('maciej_second')}}">Second</a></li>
                    <li> <b><a href="{{ path('maciej_form')}}">Form</a></b></li>
                    <li> <a href="{{ path('maciej_list')}}">List of Games</a></li>
                </ul>
            {% endblock %}
    
{% block body %} 

{{ form_start(form) }}
{{ form_widget(form) }}
        <input type="submit" value="Create"
               class="btn btn-default pull-right"/>
{{ form_end(form) }}


{% endblock %}    

